package model.enums;

public enum PoliticalFaction {
    // Default function is useful when you in game_config, you want to define a unit stats or single stats that applies
    // to all units regardless of factions.
    DEFAULT,
    // Specific factions.
    ROME, GAUL, ATHENS, SPARTA, THEBES
}
