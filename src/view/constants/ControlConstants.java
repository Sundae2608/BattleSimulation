package view.constants;

public class ControlConstants {
    // If the square distance between the unit and the mouse is smaller than this amount, then we can assign the unit.
    public final static double UNIT_ASSIGNMENT_MOUSE_DISTANCE = 200;
    public final static double UNIT_ASSIGNMENT_MOUSE_SQ_DISTANCE =
            UNIT_ASSIGNMENT_MOUSE_DISTANCE * UNIT_ASSIGNMENT_MOUSE_DISTANCE;
}
