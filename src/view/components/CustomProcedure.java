package view.components;

/**
 * This is the interface that contains a method that does some action.
 *
 * We use an interface os to give the users the ability to perform a specific action once a component (like button) is
 * pressed
 */
public interface CustomProcedure {
    /** A specific procedure to be done when a component is interacted */
    void proc();
}
